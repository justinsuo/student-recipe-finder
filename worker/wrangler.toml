name = "student-recipe-finder-api"
main = "src/index.ts"
compatibility_date = "2025-05-01"
compatibility_flags = ["nodejs_compat"]

# OPENAI_API_KEY and ALLOWED_ORIGIN are stored as worker secrets:
#   wrangler secret put OPENAI_API_KEY
#   wrangler secret put ALLOWED_ORIGIN  (e.g. https://justinsuo.github.io)

# Model overrides are plain [vars] — change in code or via:
#   wrangler secret put RECIPE_MODEL
# All overrides are optional. Defaults pick real, available OpenAI models.

[vars]
# Text models. Aliases (no dated snapshots) so OpenAI auto-resolves to the
# latest version. Override per-task to use bigger / cheaper models without
# changing code.
#
# Defaults are pinned to gpt-5-nano — the cheapest member of the GPT-5
# family (released 2025-08, ~$0.05/1M input tokens). Use gpt-5-mini for
# more headroom or gpt-5 for the flagship.
DEFAULT_TEXT_MODEL = "gpt-5-nano"
RECIPE_MODEL = "gpt-5-nano"
RECIPE_HIGH_QUALITY_MODEL = "gpt-5-nano"
LIGHTWEIGHT_MODEL = "gpt-5-nano"
PRICING_MODEL = "gpt-5-nano"
INGREDIENT_MODEL = "gpt-5-nano"
WEB_RECIPE_MODEL = "gpt-5-nano"

# Image models. dall-e-3 is the safe default — works on every OpenAI org.
# gpt-image-1 needs org verification but produces nicer images when available.
DEFAULT_IMAGE_MODEL = "dall-e-3"
IMAGE_MODEL_FALLBACK = "gpt-image-1"
